version: '3'
services:
  flask_server:
    build:
      context: ""
      dockerfile: Dockerfile
    volumes:
      - .:/app
    ports:
      - 5000:5000
    restart: always
    command: python app.py
